name: publish-npm

on:
  workflow_call:
    inputs:
      ref:
        description: "Git ref to checkout (tag or SHA)"
        required: true
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Run pytest
        run: uv run --extra dev --python 3.12 pytest # This will sync the version with python package

      - name: Setup Node.js for template build
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm for trusted publishing
        run: npm install -g npm@latest # Trusted publishing requires npm >= 11.5.1

      - name: Build web template
        working-directory: src/mjswan/template
        run: |
          npm install
          npm run build

      - name: Publish to npm
        working-directory: src/mjswan/template
        run: npm publish --access public # Auth via OIDC trusted publishing
